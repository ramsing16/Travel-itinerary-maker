請根據我提供的行程需求，生成一個可以直接匯入「Travel Itinerary Maker」網站的 JSON 檔。

重要規則：
1. 只輸出 JSON，不要輸出 Markdown、解釋文字或 ```json 程式碼框。
2. 必須完全跟隨我提供的 JSON template 結構。
3. 日期欄位 `iso` 必須使用 `YYYY-MM-DD` 格式。
4. `noteColor` 和每個 item 的 `color` 只能使用以下五個值之一：
   - `hotel`：住宿
   - `transport`：航班 / 交通 / 車程
   - `food`：美食 / 訂位
   - `shopping`：購物 / 手信 / 商場 / 藥妝 / 免稅
   - `important`：重要提醒 / 景點 / 活動
5. 每日 `items` 建議 3 至 5 個，不要過度密集。
6. 每個 item 如有明確景點、餐廳、酒店、商場或車站，請在 `places` 內填寫一個或多個地點：
   - `placeName`：店舖 / 景點 / 地點名稱
   - `address`：完整地址；不確定就留空，不要亂作
   - `googleMapUrl`：該地點的 Google Maps place 連結；不可使用 `/maps/search/`；不確定就留空
7. 請加入合理的交通、車程、用餐、休息、購物和備案時間。
8. 不要加入虛構的私人資料，例如訂單號、電話、護照號碼、QR code、門票 barcode。
9. 如果資料不足，請用合理旅遊經驗補充，但不要假裝已確認訂位或門票。
10. 請使用繁體中文。

我的行程需求如下，請按實際需要修改：

目的地：
[填寫目的地，例如：北海道 / 東京 / 大阪 / 台灣 / 首爾]

旅程日期：
[填寫日期，例如：2026年6月29日至7月4日]

同行人數 / 需要：
[填寫同行人數、長者、小朋友、行李、駕駛或步行限制]

交通方式：
[填寫自駕 / JR / 巴士 / 地鐵 / 包車 / 混合交通]

已確認資料：
[填寫已確認航班、酒店、租車、門票、餐廳、活動時間；如沒有就寫沒有]

想去的景點：
[填寫景點清單]

想吃的美食 / 餐廳：
[填寫餐廳或美食偏好]

行程節奏：
[填寫輕鬆 / 普通 / 緊湊；是否需要午休、購物時間、雨天備案]

特別要求：
[填寫例如：每天不要太早起、避免太多步行、想看日落、需要泊車資料、要親子友善]

請根據以上資料，生成完整 JSON。以下是必須跟隨的 JSON template：

[在這裡貼上 ai-itinerary-template.json 的完整內容]
